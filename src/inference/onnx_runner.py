from __future__ import annotations


def run_onnx_inference_benchmark(*args, **kwargs):
    raise RuntimeError("ONNX Runtime runner is not implemented yet. If onnxruntime is configured, implementation must be completed; no fallback is allowed.")
